# this is the code for converting an image of all folder basically  in file
# provides functions for creating and removing a directory (folder), fetching its contents,
# changing and identifying the current directory, etc. and for interacting
import os
import cv2
import glob

for filename in images:

    print(img)
    cv2.imshow('image', img)
    cv2.waitKey(5000)
    cv2.destroyAllWindows()

